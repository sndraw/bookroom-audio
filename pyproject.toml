[project]
name = "bookroom-audio"
version = "0.0.5"
description = "API for translating audio to text using a speech recognition model."
readme = "README.md"
requires-python = ">=3.11"
authors = [
    { name = "sndraw" },
]

dependencies = [
    "argparse>=1.4.0",
    "ascii-colors>=0.5.2",
    "chattts>=0.0.9",
    "dotenv>=0.9.9",
    "edge-tts>=6.1.9",
    "fastapi>=0.115.11",
    "faster-whisper>=1.1.1",
    "openai-whisper>=20240930",
    "pydub>=0.25.1",
    "python-multipart>=0.0.20",
    "pyttsx3>=2.99",
    "qwen-asr>=1.0.0",
    "torch>=2.6.0",
    "torchaudio>=2.6.0",
    "torchvision>=0.21.0",
    "uvicorn>=0.34.0",
]

[tool.setuptools.packages.find]
where = ["./"]
include = ["bookroom_audio"]
exclude = []