{
  "name": "bookroom-audio-streaming-demo",
  "version": "0.1.0",
  "private": true,
  "description": "bookroom-audio 流式 ASR 浏览器 Demo（Vite + 原生 TS）",
  "type": "module",
  "scripts": {
    "dev": "vite --host 127.0.0.1 --port 5180 --open",
    "build": "vite build",
    "preview": "vite preview --host 127.0.0.1 --port 5180 --open"
  },
  "dependencies": {
    "@bookroom/audio-sdk": "file:../sdk/typescript"
  },
  "devDependencies": {
    "typescript": "^5.4.0",
    "vite": "^5.4.0"
  }
}
